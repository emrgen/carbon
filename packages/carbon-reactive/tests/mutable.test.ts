import { test } from "vitest";

test("test mutable", async (t) => {
  // const m1 = new Mutable();
  // m1.define("a", 0);
  // m1.accessor("a")["a"] = 1;
  // expect(m1.variables.get("a")).toEqual(1);
  //
  // m1.delete("a");
  //
  // expect(() => {
  //   m1.accessor("a")["a"];
  // }).toThrowError("not defined");
});
