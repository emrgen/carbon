export enum BlockMenuCmd {
  check = 'blockMenu:check',
  hide = 'blockMenu:hide',
  scrollDown = 'blockMenu:scrollDown',
  scrollUp = 'blockMenu:scrollUp',
  select = 'blockMenu:select',
  show = 'blockMenu:show',
}
