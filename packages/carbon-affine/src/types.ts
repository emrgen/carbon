export type Radian = number;
export type Degree = number;
export type Scale = number;


// Transform types for affine transformations
export enum TransformType {
  TRANSLATE = "translate",
  ROTATE = "rotate",
  SCALE = "scale",
}
