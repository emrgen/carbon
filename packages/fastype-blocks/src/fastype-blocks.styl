[data-name=callout] [data-type=content]
  display: flex
  gap: 12px
  [data-name=title]
    flex 1
[data-name=callout]
	padding: 12px 12px

.fastype-callout-icon
  display: flex
  align-items: center
